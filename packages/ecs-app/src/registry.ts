import { defineResource } from "define";

type TypeRegistry = any;

export const AppTypeRegistry = defineResource(class AppTypeRegistry {
})


export const AppFunctionRegistry = defineResource(class AppFunctionRegistry {
})